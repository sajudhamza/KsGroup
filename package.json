{
  "name": "ks-hospitality-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "KS Hospitality Group — marketing site (Vite + React).",
  "scripts": {
    "prepare-fishbowl": "python3 scripts/prepare-fishbowl.py",
    "prepare-rickey": "python3 scripts/prepare-rickey.py",
    "prepare-maine-grill": "python3 scripts/prepare-maine-grill.py",
    "prepare-microsites": "python3 scripts/prepare-microsites.py",
    "dev": "npm run prepare-microsites && vite",
    "build": "npm run prepare-microsites && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
